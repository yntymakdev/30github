"use client";

import { useMemo } from "react";

interface EditorProps {
  onChange: (value: string) => void;
  value: string;
}

export const Edtor = () => ({
  onChange,
  value,
}:EditorProps) => {
    const   ReactQuil = useMemo((=> ))
};
